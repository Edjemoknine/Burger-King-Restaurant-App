import React from "react";

const UpdatePro = ({ id }: { id: string }) => {
  return (
    <button className=" rounded-lg px-1.5 py-1 text-xs bg-teal-500 hover:bg-teal-600 duration-300">
      Edit
    </button>
  );
};

export default UpdatePro;
